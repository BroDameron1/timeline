<div class="form-field">
    <label for="title">Title</label>
    <input type="text" id="title" name="title" value="<%= data.title %>" required autofocus>
</div>
<div class="form-field">
    <label for="mediaType">Media Type</label>
    <select name="mediaType" id="mediaType" disabled>
        <% for(let mediaType of mediaTypes) { %>
            <option value="<%= mediaType %>" <%= data.mediaType === mediaType ? 'selected': ''%>><%= mediaType %></option>
        <% } %> 
    </select>
    <input type="hidden" name="mediaType" value="<%= data.mediaType %>">
</div>
<div class="form-field">
    Upload Image
    <div class="file-name" id="file-name"></div>
    <input type="file" name="sourceImage" id="sourceImage" class="inputfile">
    <label for="sourceImage" class="btn-browse">Browse...</label>
</div>
<div class="imgpreview-wrapper">
    <img src="<%= data.images.url %>" class="image-preview">
</div>  


<div class="hide-sources" id="book-fields">
    <div class="form-field">
        <label for="book-author">Author(s)</label>
        <% if (data.book.author && data.book.author.length > 0) { %> 
            <% for(let author of data.book.author) { %>    
                <div class="form-field"><input type="text" class="book-author" name="book[author][]" value="<%= author %>"></div>
            <% } %>
        <% } else { %>
            <input type="text" class="book-author" name="book[author][]">
        <% } %> 
        <a href="#" id="add-author">Add another author</a>
    </div>
    <div class="form-field">
        <label for="book-series">Series</label>
        <input type="text" id="book-series" name="book[series]" value="<%= data.book.series %>">
    </div>
    <div class="form-field">
        <label for="publisher">Publisher</label>
        <input type="text" id="publisher" name="book[publisher]" value="<%= data.book.publisher %>">
    </div>
    <div class="form-field">
        <label for="bookReleaseDate">Initial Release Date</label>
        <input type="date" class="date" id="bookReleaseDate" name="book[releaseDate]" min="1977-05-25" value="<%= data.book.releaseDate %>">
    </div>
    <div class="form-field">
        <label for="isbn10">ISBN-10</label>
        <input type="text" id="isbn10" name="book[isbn10]" value="<%= data.book.isbn10 %>">
    </div>
</div> 


<div class="hide-sources" id="movie-fields">
    <div class="form-field">
        <label for="movie-director">Director(s)</label>
        <% if (data.movie.director && data.movie.director.length > 0) { %> 
            <% for(let director of data.movie.director) { %>    
                <div class="form-field"><input type="text" class="movie-director" name="movie[director][]" value="<%= director %>"></div>
            <% } %>
        <% } else { %>
            <input type="text" class="movie-director" name="movie[director][]">
        <% } %> 
        <a href="#" id="add-director">Add another director</a>
    </div>
    <div class="form-field">
        <label for="movie-writer">Writer(s)</label>
        <% if (data.movie.writer && data.movie.writer.length > 0) { %> 
            <% for(let writer of data.movie.writer) { %>    
                <div class="form-field"><input type="text" class="movie-writer" name="movie[writer][]" value="<%= writer %>"></div>
            <% } %>
        <% } else { %>
            <input type="text" class="movie-writer" name="movie[writer][]">
        <% } %> 
        <a href="#" id="add-writer">Add another writer</a>
    </div>
    <div class="form-field">
        <label for="movieReleaseDate">Initial Release Date</label>
        <input type="date" class="date" id="movieReleaseDate" name="movie[releaseDate]" min="1977-05-25" value="<%= data.movie.releaseDate %>">
    </div>
</div> 

<div class="hide-sources" id="comic-fields">
    <div class="form-field">
        <label for="comic-writer">Writer</label>
        <input type="text" id="comic-writer" name="comic[writer]" value="<%= data.comic.writer %>">
    </div>
    <div class="form-field">
        <label for="comic-artist">Art Contributor(s)</label>
        <% if (data.comic.artist && data.comic.artist.length > 0) { %> 
            <% for(let artist of data.comic.artist) { %>    
                <div class="form-field"><input type="text" id="comic-artist" class="comic-artist" name="comic[artist][]" value="<%= artist %>"></div>
            <% } %>
        <% } else { %>
            <input type="text" id="comic-artist" class="comic-artist" name="comic[artist][]">
        <% } %> 
        <a href="#" id="add-artist">Add another art contributor</a>
    </div>
    <div class="form-field">
        <label for="comic-series">Series</label>
        <input type="text" id="comic-series" name="comic[series]" value="<%= data.comic.series %>">
    </div>
    <div class="form-field">
        <label for="comic-issue">Issue</label>
        <input type="number" id="comic-issue" name="comic[issueNum]" value="<%= data.comic.issueNum %>" min="0">
    </div>
    <div class="form-field">
        <label for="comicReleaseDate">Initial Release Date</label>
        <input type="date" class="date" id="comicReleaseDate" name="comic[releaseDate]" min="1977-05-25" value="<%= data.comic.releaseDate %>">
    </div>
</div> 

<div class="hide-sources" id="tv-fields">
    <div class="form-field">
        <label for="tv-series">Series</label>
        <input type="text" id="tv-series" name="tv[series]" value="<%= data.tv.series %>">
    </div>
    <div class="form-field">
        <label for="tv-season">Season</label>
        <input type="number" id="tv-season" name="tv[season]" min="1" value="<%= data.tv.season %>">
    </div>
    <div class="form-field">
        <label for="tv-episode">Episode</label>
        <input type="number" id="tv-episode" name="tv[episode]" min="1" value="<%= data.tv.episode %>">
    </div>
    <div class="form-field">
        <label for="tvReleaseDate">Initial Release Date</label>
        <input type="date" class="date" id="tvReleaseDate" name="tv[releaseDate]" min="1977-05-25" value="<%= data.tv.releaseDate %>">
    </div>
</div> 

<div class="hide-sources" id="game-fields">
    <div class="form-field">
        <label for="vg-studio">Production Studio</label>
        <input type="text" id="vg-studio" name="videoGame[studio]" value="<%= data.videoGame.studio %>">
    </div>
    <div class="form-field">
        <label for="vg-publisher">Publisher</label>
        <input type="text" id="vg-publisher" name="videoGame[publisher]" value="<%= data.videoGame.releaseDate %>">
    </div>
    <div class="form-field">
        <label for="vgReleaseDate">Initial Release Date</label>
        <input type="date" class="date" id="vgReleaseDate" name="videoGame[releaseDate]" min="1977-05-25" value="<%= data.videoGame.releaseDate %>">
    </div>
</div> 